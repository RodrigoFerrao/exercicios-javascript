/* let cidades = [' Salvador', ' Rio de Janeiro', ' São Paulo', ' Madrid', ' Porto'];
console.log('O número de cidades registradas é ' + cidades.length);
console.log('Essas são as cidades cadastradas no sistema ' + cidades);
cidades.push(' Lisboa')
console.log('O número de cidades registradas é ' + cidades.length);
console.log('Essas são as cidades cadastradas no sistema ' + cidades);


let novasCidades = [' Feira de Santana', ' Itajaí'];
cidades.forEach((localidade, index) =>{
    console.log('O destino de número ' + index +' é ' + localidade)
})
 */
for (let contador = 0; contador < 10; contador++) {
    console.log(contador)
}

let cidades = ['Salvador', 'Rio de Janeiro', 'Porto', 'Lisboa'];
for (let indice = 0 ;indice  <  cidades.length; indice++){
    console.log(cidades[indice])
}